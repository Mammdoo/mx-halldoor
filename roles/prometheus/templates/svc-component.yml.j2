apiVersion: v1
kind: Service
metadata:
  name: "{{ project.name }}-{{ item.name }}"
  namespace: "{{ project.namespace }}"
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: {{ item.port }}
  selector:
    {{ item.name }}: {{ project.name }}