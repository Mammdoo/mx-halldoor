apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: "{{ project.name }}"
  namespace: "{{ project.namespace }}"
rules:
- apiGroups: [""]
  resources:
  - nodes
  - services
  - endpoints
  - pods
  verbs: ["get", "list", "watch"]