apiVersion: v1
kind: Service
metadata:
  name: "{{ project.name }}-prometheus"
  namespace: "{{ project.namespace }}"
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 9090
  selector:
    prometheus: {{ project.name }}